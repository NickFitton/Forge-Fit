import React from 'react';
import { Text, SafeAreaView } from 'react-native';
import { Screen } from '../../components/Screen';

export default function Exercises() {
  return (
    <Screen>
      <Text style={{ color: '#eee' }}>
        Dummy screen that needs to be replaced
      </Text>
    </Screen>
  );
}
